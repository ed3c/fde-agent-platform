# Shared Skill Binding

This consumer repository resolves canonical procedures from `ed3c/skills-shared` and does not vendor or shadow their `SKILL.md` bodies.

| Skill | Bound procedure | Consumer use |
|---|---|---|
| `git-town-stacked-pr-worker` | branch graph, path leases, bounded no-push sync, separated publication evidence | `.git-town.toml`, repo profile, stack index, work packets |
| `dual-forge-repository-loop` v1.2.0 / v3 | GitHub ingress vs local Forgejo implementation authority | runtime identity and draft binding |
| `spatial-loop-systems-engineering` v2.1.0 | Level C constraint model and default MONITOR loop | architecture SSOT, invariants, unknowns, checkpoints |
| `procedural-shadow-runtime` | pre-side-effect procedure delta and exact evidence ceilings | explicit `ABSENT` / `NOT_EXERCISED` lanes |
| `agentic-tech-lead-orchestration` v1.0.0 | contract-first DAG and serial stack | task contract and path-disjoint leases |

## Applied procedure delta

- Runtime identity was bound before mutation.
- Repository/open-issue/open-PR state was observed.
- The public/private boundary and Human-owned transitions were fixed before code.
- Issues #2–#4 define branch parents, path leases, evals, negative controls, and rollback.
- The branch stack is declared but live Git Town and Forgejo execution are not claimed.
